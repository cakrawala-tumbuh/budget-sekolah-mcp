# Tooling test (linter + unit) dengan versi DIPIN agar lokal == CI.
# Lihat skill automated-test / prinsip-parity: tanpa pin, image bulan depan
# bisa berbeda dari hari ini sehingga hasil test menyimpang.
pytest==9.0.3
pytest-asyncio==1.4.0
respx==0.23.1
ruff==0.15.15
